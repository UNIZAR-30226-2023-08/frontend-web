name: Docker Image CI

on:
  push:
    branches: [ "main", "rc" ]
  pull_request:
    branches: [ "main", "rc" ]
    
  workflow_dispatch:

jobs:

  build:

   runs-on: ubuntu-latest

   steps:
    - name: Checkout
      uses: actions/checkout@v3
    
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag frontend-web-proysw:latest
      
    - uses: ishworkh/docker-image-artifact-upload@v1
      name: Upload image
      with:
        image: "frontend-web-proysw:latest"

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Install SSH Key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_PRIVATE_KEY }} 
          known_hosts: 'placeholder'
        
      - name: Adding Known Hosts
        run: ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts

      - name: Download image
        id: img_download
        uses: ishworkh/docker-image-artifact-download@v1
        with:
          image: "frontend-web-proysw:latest"

      - name: Send image over scp
        run: scp "${{ steps.img_download.outputs.download_path }}" ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }}:/tmp/docker_img

      - name: Load image
        run: ssh ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }} docker load -i /tmp/docker_img && rm /tmp/docker_img
